{% extends 'SimpleProfileBundle::layout.html.twig' %}


{% block body -%}
<h1>Kreiraj osobu</h1>

<form action="{{ path('profil_upisi') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('profil_ispis') }}">
            Nazad na listu
        </a>
    </li>
</ul>
{% endblock %}